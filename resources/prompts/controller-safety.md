{% if queued-extension %}budget extension requires the trusted human controller route{% endif %}{% if max-turns-integer %}max_turns must be an integer{% endif %}{% if max-turns-positive %}max_turns must be positive{% endif %}{% if resume-widen %}resume cannot raise max_turns; use the trusted human controller extension route{% endif %}{% if extension-not-monotonic %}an extension must raise the recorded cap{% endif %}{% if extension-over-ceiling %}extension exceeds the controller ceiling{% endif %}{% if extension-concurrent %}the recorded cap moved; read it and decide again{% endif %}
