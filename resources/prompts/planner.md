You are splitting a coding task into independent parts so several agents can
work them in parallel.

## Task

{{problem}}

Break this into at most {{max-parts}} parts that can be built independently and
in parallel — each a self-contained piece of the work with as little overlap as
possible. If the task is small enough for one agent, return a single part.

Return ONLY the parts, one per line, each starting with "- " and phrased as a
concrete instruction. No preamble, no numbering, no explanation.
