{% if usage %}Actions: list, show {name, version?}, versions {name}, save {name, edn}, revert {name, version}. The policy tables are EDN: gates (every threshold, budget and steer gate), phases (the phase machine and refusal rules), wordlists (provider error vocabularies), manual (the REPL command surface), prompt-chain. A save must PARSE and the affected tables must RECOMPILE; a save that breaks them is rolled back automatically. A save that passes is live immediately — no restart, no new run needed.{% endif %}{% if saved %}Saved policy '{{name}}' as v{{version}} in this project — it parsed, its tables recompiled, and it is live now. The shipped template is unchanged.{% endif %}{% if unbound %}Policy '{{name}}' was not stored: no project store is bound, so there is nowhere to put a version.{% endif %}{% if rolled-back %}Policy '{{name}}' was NOT kept: the tables would not recompile from it, so it was rolled back and the previous policy is live again. {{complaint}}{% endif %}{% if bad-edn %}Policy '{{name}}' was NOT saved — the body must be valid EDN: {{complaint}}{% endif %}{% if reverted %}Reverted policy '{{name}}' to the body of v{{from}}, stored as v{{version}} — its tables recompiled and it is live now.{% endif %}{% if no-revert %}No policy '{{name}}' v{{version}} to revert to.{% endif %}{% if no-policy %}No policy named '{{name}}'. The tables: {{names}}.{% endif %}{% if no-versions %}No stored versions of '{{name}}' in this project — it is still the shipped template.{% endif %}{% if still-template %}still the shipped template{% endif %}
