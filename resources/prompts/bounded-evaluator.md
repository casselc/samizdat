{% if completion-refused %}Completion refused: controller verification is unavailable in M1.{% endif %}{% if outside-tool %}Tool `{{tool}}` is outside this bounded context. Available: eval, doc, complete, done.{% endif %}{% if missing-binding %}Bounded evaluation binding is unavailable; refusing ordinary eval fallback.{% endif %}{% if runtime-unavailable %}Pinned bounded evaluator runtime is unavailable.{% endif %}{% if no-callable %}No callable name {{symbol}} exists in this bounded context.{% endif %}{% if absolute-path %}Absolute paths are outside the bounded context.{% endif %}{% if path-escape %}Project path escapes the bounded root.{% endif %}{% if symlink-path %}Symbolic links are refused in every path component.{% endif %}{% if path-missing %}Project path does not exist under the bounded root.{% endif %}{% if root-not-file %}The project root is not a file.{% endif %}{% if read-not-file %}project/read target is not a regular file.{% endif %}{% if read-large %}project/read exceeded its character bound.{% endif %}{% if read-not-utf8 %}project/read target is not valid UTF-8.{% endif %}{% if list-not-dir %}project/list target is not a directory.{% endif %}{% if list-many %}project/list exceeded its entry bound.{% endif %}{% if search-args %}project/search expects a pattern and an optional options map.{% endif %}{% if search-not-dir %}project/search target is not a directory.{% endif %}{% if search-many %}project/search exceeded its file bound.{% endif %}{% if stat-digest-failed %}project/stat could not compute a deterministic digest.{% endif %}{% if eval-timeout %}Evaluation exceeded its per-evaluation timeout.{% endif %}{% if history-gap %}Evaluator history has a sequence gap.{% endif %}{% if evaluator-busy %}Evaluator instance is already evaluating.{% endif %}{% if orientation-envelope %}CALLING A TOOL. Every turn that acts must end with exactly one top-level tool call, written as a fenced block:

```tool-call
{"name": "eval", "args": {"code": "(project/read \"src/foo.clj\")"}}
```

Inside that JSON every `"` is `\"` and every newline is `\n`. For multi-line content do not fight the escaping — use the XML form, whose parameter values are raw text:

<invoke name="eval"><parameter name="code">(project/read "src/foo.clj")</parameter></invoke>

One form per reply, one call per turn.{% endif %}

{% if orientation-in-eval %}{{ops}} are ordinary Clojure calls available ONLY INSIDE eval. They are NOT top-level tool names.

RIGHT: {"name": "eval", "args": {"code": "({{example-op}} \"src/foo.clj\")"}}
WRONG: {"name": "{{example-op}}", "args": {"path": "src/foo.clj"}}{% endif %}

{% if orientation-guidance %}SCI is persistent and programmable, and it keeps its state across turns and across a controller restart: anything you `def` is reconstructed from the durable history and can be reused later. Use doc/complete to discover the surface, combine several read-only observations in one eval, branch/filter/aggregate locally, and define small helpers instead of rebuilding them each turn. An observation you already made returns the same value until something changes it — reuse the result you have rather than re-reading.{% endif %}{% if orientation-develop %}

CHANGING A FILE. `base` is always the exact digest project/stat returned for the current content.

- Changing part of an existing file — the usual case — use the anchored form `(project/edit path base old-text new-text)`. It replaces the ONE exact occurrence of old-text and leaves every other byte alone. old-text must occur exactly once; zero or several occurrences are refused, so widen or narrow the anchor until it is unique. Prefer this: you do not have to reproduce the rest of the file, and you cannot damage what you did not name.
- Creating a file — `(project/edit path :absent new-content)`.
- Replacing a whole existing file — `(project/edit path base new-content)`. Only when you genuinely mean to rewrite all of it. Rewriting a file from memory is how unrelated definitions get deleted.

A stale base, a missing anchor target, or an existing create target is refused and writes nothing — re-stat, rebase on the fresh digest, and retry.{% endif %}{% if replace-args %}project/edit's anchored form expects a relative path, a base of the exact "sha256:..." digest from project/stat, and non-empty old-text and new-text strings.{% endif %}{% if replace-missing %}project/edit found no occurrence of old-text in the file; nothing was written. Re-read the file and anchor on text that is actually there.{% endif %}{% if replace-ambiguous %}project/edit found more than one occurrence of old-text; nothing was written. Widen the anchor with surrounding context until it names exactly one place.{% endif %}{% if edit-args %}project/edit expects a relative path, a base of :absent or an exact "sha256:..." digest from project/stat, and string content.{% endif %}{% if edit-protected %}project/edit target is the operator's run config; the run it gates may not edit it.{% endif %}{% if edit-not-file %}project/edit target is not a regular file.{% endif %}{% if edit-stale %}project/edit base digest does not match the current content; re-stat and rebase.{% endif %}{% if edit-missing %}project/edit base names a digest, but the path does not exist; pass :absent to create.{% endif %}{% if edit-existing %}project/edit base is :absent (create), but the path exists; anchor on its project/stat digest instead.{% endif %}{% if edit-large %}project/edit content exceeded its character bound.{% endif %}{% if edit-replace %}project/edit could not complete its atomic replace; the target is untouched.{% endif %}{% if done-nothing-changed %}Completion refused: this run changed no project files, so there is nothing to verify. Make the change with project/edit, pin it with a test the focused verifier can run, then call done again.{% endif %}{% if done-no-verifiable-test %}Completion refused: none of this run's changed files is a test the controller can verify — a test-shaped path whose name falls outside the namespace whitelist is refused, never trusted. Pin the change with a plainly named test under the project's test tree, then call done again.{% endif %}{% if ve-truncated %}
… [verifier output truncated at the capture bound]
{% endif %}{% if ve-unavailable %}The verification environment is unavailable.{% endif %}{% if ve-stage-failed %}The verification environment failed to stage its private workspace: the controller could not create a private root.{% endif %}{% if ve-run-failed %}The verification environment failed to run: {{reason}}{% endif %}{% if ve-no-verifier %}The pinned verifier executable `{{exec}}` cannot be resolved by the controller.{% endif %}{% if ve-no-test %}No verifiable test among the run's changed files.{% endif %} {% if done-verify-env-unavailable %}Completion refused: the controller's verification environment is unavailable ({{reason}}). Bounded completion is refused rather than run unsandboxed; the selected environment's own substrate is missing (for the bubblewrap environment: Linux with bubblewrap, prlimit, unprivileged user namespaces and a resolvable pinned verifier; for the SmolVM environment: the measured smolvm manager, a pinned guest image and a working virtualization substrate). It is not a test failure — nothing you wrote caused it.{% endif %} {% if done-closure-red %}Completion refused. Your focused tests are green, but the controller then ran the project's WHOLE suite over your changes and it is not green. Something you changed broke something outside the namespace your own test covers. Read the failure below, fix it, and call done again — your focused tests still have to stay green.{% endif %}{% if done-green %}Completion accepted: the controller's focused verification of this run's changed tests is green, and its broader closure verification of the project's whole suite is green.{% endif %}
{% if edit-create-unsupported %}Atomic no-replace create is unsupported on this host.{% endif %}{% if edit-create-failed %}Atomic no-replace create failed.{% endif %}{% if stale-turn %}Turn authority expired; this stale tool call was not dispatched.{% endif %}{% if binding-missing %}Bounded resume has no durable EvaluatorBinding.{% endif %}{% if context-coordinate-mismatch %}Durable ContextSpec coordinate does not match its content.{% endif %}{% if root-mismatch %}Bounded resume root differs from the durable ContextSpec.{% endif %}{% if orientation-missing %}Durable binding carries no trusted orientation to restore.{% endif %}{% if orientation-digest-mismatch %}Durable trusted-orientation bytes do not match their digest.{% endif %}
