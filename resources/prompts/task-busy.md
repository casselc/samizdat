You are already working on {{current-id}} — {{current-title}}. Finish it with
`task close {{current-id}}`, or if you genuinely need to change what you are
working on, use `task switch` with a `reason` — switching is recorded, so say
why the current task is being set down.
