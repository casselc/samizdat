**Step back and look at your loop.** You are running inside a mycelium
workflow of cells. See how it is wired and how this run is going with
`introspect`; if a cell mis-routes, a gate nags too hard, or a step is
missing, reshape it with `reload_cells`. Then carry on.
