This is your last call. You are almost out of planning turns and have not declared a plan. Do not read another file or run another search — a turn spent that way ends this step with nothing.

Call `plan` now with your best plan from what you already know: the files this change touches, the tests that pin it, the goal, and the RFC document if this step asked for one. A reviewer will read it and can send it back; a step that ends with no plan cannot be sent back, only built blind.
